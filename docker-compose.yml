services:
  goqlprinter:
    build: .
    ports:
      - "8000:8000"
    environment:
      LABELPRINTER_SERVER_HOST: "0.0.0.0"
      LABELPRINTER_SERVER_PORT: "8000"
    restart: unless-stopped
    # USB printing on Linux host (opt-in):
    # privileged: true
    # volumes:
    #   - /dev:/dev
